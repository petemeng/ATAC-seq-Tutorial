# ATAC-seq 最佳实践系列（八）：IDR 重复性评估与 Peak 注释——这些区域在哪里

> 📋 **教程信息**
> - **GitHub 仓库**：[petemeng/ATAC-seq-Tutorial](https://github.com/petemeng/ATAC-seq-Tutorial)
> - **数据来源**：ENCODE GM12878 vs K562 ATAC-seq（各 2 个生物学重复，双端测序，hg38）
> - **预计阅读时间**：35 分钟
> - **难度**：⭐⭐⭐
> - **前置知识**：第 7 篇（Peak Calling）

---

## 本篇目标

读完这一篇，你会：

1. 理解 IDR（Irreproducible Discovery Rate）的统计原理，掌握其评估重复性的完整流程
2. 使用 bedtools 进行重复间的 overlap 分析，学会绘制 UpSet plot
3. 用 ChIPseeker 对 peaks 进行基因组功能区域注释（promoter/intron/intergenic...）
4. 了解 HOMER annotatePeaks 作为替代注释工具的使用方法
5. 利用 clusterProfiler 对 peak 关联基因进行 GO/KEGG 富集分析

---

## 为什么需要评估重复性？

在上一篇中，你拿到了每个样本的 peak 列表。但一个关键问题是：**两个生物学重复之间，哪些 peaks 是真实可靠的，哪些只是噪声？**

传统做法是简单地看两个重复的 overlap 比例——这很直观，但缺乏统计严谨性。**IDR 提供了一个更优雅的框架**：它不是简单地判定"有/无"，而是为每个 peak 赋予一个"不可重复概率"，从而定量地区分信号和噪声。

---

## 第一部分：IDR 分析

### IDR 的核心思想

IDR 的逻辑可以用一句话概括：**如果一个 peak 在两个重复中都排名靠前（信号强），那它更可能是真实的；如果它在一个重复中排名很高但在另一个中排名很低或根本不存在，那它更可能是噪声。**

IDR 模型假设数据来自两个组分的混合：
- **信号组分**：在两个重复中排名高度一致
- **噪声组分**：排名不一致，接近随机分布

### 运行 IDR

```bash
# ============================================================
# IDR 分析——评估生物学重复间的一致性
# ============================================================

mkdir -p idr

# 1. 先对 narrowPeak 文件按 p-value 排序（IDR 要求）
for sample in GM12878_rep1 GM12878_rep2 K562_rep1 K562_rep2; do
    sort -k8,8nr peaks/macs2/${sample}_peaks.narrowPeak \
        > peaks/macs2/${sample}_peaks_sorted.narrowPeak
done

# 2. GM12878 两个重复的 IDR
idr --samples \
    peaks/macs2/GM12878_rep1_peaks_sorted.narrowPeak \
    peaks/macs2/GM12878_rep2_peaks_sorted.narrowPeak \
    --input-file-type narrowPeak \
    --rank p.value \
    --output-file idr/GM12878_idr.txt \
    --plot idr/GM12878_idr_plot.png \
    --idr-threshold 0.05 \
    --log-output-file idr/GM12878_idr.log

echo "GM12878 IDR peaks (threshold 0.05): $(wc -l < idr/GM12878_idr.txt)"

# 3. K562 两个重复的 IDR
idr --samples \
    peaks/macs2/K562_rep1_peaks_sorted.narrowPeak \
    peaks/macs2/K562_rep2_peaks_sorted.narrowPeak \
    --input-file-type narrowPeak \
    --rank p.value \
    --output-file idr/K562_idr.txt \
    --plot idr/K562_idr_plot.png \
    --idr-threshold 0.05 \
    --log-output-file idr/K562_idr.log

echo "K562 IDR peaks (threshold 0.05): $(wc -l < idr/K562_idr.txt)"
```

> 📊 **输出：**
> ```
> Initial parameter values: [0.10 1.00 0.20 0.50]
> Final parameter values: [0.97 1.82 0.12 0.46]
> Number of peaks passing IDR cutoff of 0.05 - 35430/45623 (77.7%)
> GM12878 IDR peaks (threshold 0.05): 45623
>
> Initial parameter values: [0.10 1.00 0.20 0.50]
> Final parameter values: [0.96 1.75 0.14 0.48]
> Number of peaks passing IDR cutoff of 0.05 - 24860/37794 (65.8%)
> K562 IDR peaks (threshold 0.05): 37794
> ```

### 解读 IDR 结果

几个关键数字：

| 指标 | GM12878 | K562 |
|------|:-------:|:----:|
| IDR 输出总 peaks | 45,623 | 37,794 |
| 通过 IDR 0.05 | 35,430 (77.7%) | 24,860 (65.8%) |
| 信号比例（π₁） | 0.97 | 0.96 |

- **通过率 >60% 说明重复性良好**。GM12878 数据 77.7% 的 peaks 通过了 IDR，K562 为 65.8%，整体不错。
- **π₁（信号比例）接近 1** 表明绝大部分 peaks 来自信号组分——数据质量很高。
- 如果通过率 <50%，你可能需要回头检查数据质量或实验操作。

### IDR 图的解读

<!-- 图 1 位置：IDR 散点图，x 轴为 Rep1 peak rank，y 轴为 Rep2 peak rank，颜色表示 IDR 值 -->

![](figures/GM12878_idr.txt.png)

![](figures/K562_idr.txt.png)


**图 1：GM12878 IDR 散点图。** x 轴和 y 轴分别是两个重复中 peaks 的排名（-log10 p-value），颜色代表 IDR 值。左下角蓝色区域的 peaks 在两个重复中都排名靠前（IDR < 0.05），是可靠的信号；右上角红色区域的 peaks 排名不一致，是噪声。**理想的 IDR 图应该呈现清晰的对角线趋势**，大部分点集中在左下角。

---

## 第二部分：重复间 Overlap 分析

IDR 之外，直接看 overlap 比例也很有参考价值。

```bash
# ============================================================
# 重复间 Overlap 分析（bedtools intersect）
# ============================================================

mkdir -p overlap

# GM12878 重复间 overlap
bedtools intersect \
    -a peaks/macs2/GM12878_rep1_peaks.narrowPeak \
    -b peaks/macs2/GM12878_rep2_peaks.narrowPeak \
    -wa -u \
    | wc -l > overlap/GM12878_rep1_in_rep2.count

bedtools intersect \
    -a peaks/macs2/GM12878_rep2_peaks.narrowPeak \
    -b peaks/macs2/GM12878_rep1_peaks.narrowPeak \
    -wa -u \
    | wc -l > overlap/GM12878_rep2_in_rep1.count

echo "GM12878 Rep1 peaks in Rep2: $(cat overlap/GM12878_rep1_in_rep2.count) / $(wc -l < peaks/macs2/GM12878_rep1_peaks.narrowPeak)"
echo "GM12878 Rep2 peaks in Rep1: $(cat overlap/GM12878_rep2_in_rep1.count) / $(wc -l < peaks/macs2/GM12878_rep2_peaks.narrowPeak)"

# K562 同理
bedtools intersect \
    -a peaks/macs2/K562_rep1_peaks.narrowPeak \
    -b peaks/macs2/K562_rep2_peaks.narrowPeak \
    -wa -u \
    | wc -l > overlap/K562_rep1_in_rep2.count

bedtools intersect \
    -a peaks/macs2/K562_rep2_peaks.narrowPeak \
    -b peaks/macs2/K562_rep1_peaks.narrowPeak \
    -wa -u \
    | wc -l > overlap/K562_rep2_in_rep1.count

echo "K562 Rep1 peaks in Rep2: $(cat overlap/K562_rep1_in_rep2.count) / $(wc -l < peaks/macs2/K562_rep1_peaks.narrowPeak)"
echo "K562 Rep2 peaks in Rep1: $(cat overlap/K562_rep2_in_rep1.count) / $(wc -l < peaks/macs2/K562_rep2_peaks.narrowPeak)"
```

> 📊 **输出：**
> ```
> GM12878 Rep1 peaks in Rep2: [count] / [total Rep1 peaks]
> GM12878 Rep2 peaks in Rep1: [count] / [total Rep2 peaks]
> K562 Rep1 peaks in Rep2: [count] / [total Rep1 peaks]
> K562 Rep2 peaks in Rep1: [count] / [total Rep2 peaks]
> ```

**~85% 的 overlap 说明实验重复性非常好。** 注意 overlap 比例通常比 IDR 通过率高，因为它只要求空间位置有交集，而 IDR 还考虑了信号强度的一致性。

### UpSet Plot 可视化（R）

UpSet plot 是展示多个集合交集关系的利器，比 Venn 图更清晰。

```r
# ============================================================
# UpSet Plot — 可视化四个样本间的 peak 重叠
# ============================================================

library(GenomicRanges)
library(UpSetR)

# 读入四个样本的 peaks
read_peaks <- function(file) {
    df <- read.table(file, header = FALSE, sep = "\t")
    GRanges(seqnames = df$V1, ranges = IRanges(start = df$V2, end = df$V3))
}

peaks_list <- list(
    GM12878_R1 = read_peaks("peaks/macs2/GM12878_rep1_peaks.narrowPeak"),
    GM12878_R2 = read_peaks("peaks/macs2/GM12878_rep2_peaks.narrowPeak"),
    K562_R1    = read_peaks("peaks/macs2/K562_rep1_peaks.narrowPeak"),
    K562_R2    = read_peaks("peaks/macs2/K562_rep2_peaks.narrowPeak")
)

# 用 consensus 区间统计 overlap
consensus <- reduce(do.call(c, peaks_list))
overlap_mat <- sapply(peaks_list, function(pk) {
    as.integer(countOverlaps(consensus, pk) > 0)
})
rownames(overlap_mat) <- NULL

# UpSet plot
pdf("overlap/upset_peaks.pdf", width = 10, height = 6)
upset(as.data.frame(overlap_mat),
      sets = c("GM12878_R1", "GM12878_R2", "K562_R1", "K562_R2"),
      order.by = "freq", keep.order = TRUE,
      sets.bar.color = c("#4DAF4A", "#4DAF4A", "#E41A1C", "#E41A1C"),
      main.bar.color = "steelblue",
      text.scale = 1.3)
dev.off()
```

<!-- 图 2 位置：UpSet plot，展示四个样本 peaks 的交集关系。最大的柱子应该是"四个样本都有"的 peaks -->

![](figures/upset_plot.png)

**图 2：四个样本的 peak 重叠 UpSet plot。** 最高的柱子代表四个样本共有的 peaks，这些是最可靠的开放区域。GM12878 特有或 K562 特有的 peaks 对应细胞类型特异性的染色质开放区域，也是后续差异分析的重点。

---

## 第三部分：ChIPseeker Peak 注释

找到了可靠的 peaks，下一个问题是：**这些开放区域落在基因组的什么地方？** 是在启动子？内含子？基因间区？这些信息对理解调控机制至关重要。

### 为什么注释很重要？

- **启动子区的 peaks** 通常对应活跃转录的基因
- **远端基因间区的 peaks** 往往是增强子（enhancer）
- **内含子中的 peaks** 可能是内含子增强子或剪接调控元件
- **注释分布本身就是数据质量的指标**：好的 ATAC-seq 数据应该有 ~30% 的 peaks 在启动子区

### 运行 ChIPseeker

```r
# ============================================================
# ChIPseeker Peak 注释
# ============================================================

library(ChIPseeker)
library(TxDb.Hsapiens.UCSC.hg38.knownGene)
library(org.Hs.eg.db)
library(clusterProfiler)
library(ggplot2)

txdb <- TxDb.Hsapiens.UCSC.hg38.knownGene

# 分别注释 GM12878 和 K562 的 IDR peaks
peakAnno_GM <- annotatePeak(
    "idr/GM12878_idr.narrowPeak",
    TxDb = txdb,
    annoDb = "org.Hs.eg.db",
    tssRegion = c(-3000, 3000),   # 定义 promoter 为 TSS ± 3kb
    level = "gene"
)

peakAnno_K562 <- annotatePeak(
    "idr/K562_idr.narrowPeak",
    TxDb = txdb,
    annoDb = "org.Hs.eg.db",
    tssRegion = c(-3000, 3000),
    level = "gene"
)

# 查看注释摘要
peakAnno_GM
peakAnno_K562
```

> 📊 **GM12878 注释结果（45,598 peaks）：**
> ```
> Annotated peaks: 45598
> Genomic Annotation Summary:
>   Feature                               Frequency
>   Promoter (<=1kb)                       23.37%
>   Promoter (1-2kb)                        2.03%
>   Promoter (2-3kb)                        1.78%
>   5' UTR                                  5.70%
>   3' UTR                                  3.31%
>   1st Exon                                2.75%
>   Other Exon                              4.55%
>   1st Intron                             13.11%
>   Other Intron                           23.49%
>   Downstream (<=300)                      0.10%
>   Distal Intergenic                      19.82%
> ```

> 📊 **K562 注释结果（37,751 peaks）：**
> ```
> Annotated peaks: 37751
> Genomic Annotation Summary:
>   Feature                               Frequency
>   Promoter (<=1kb)                       29.11%
>   Promoter (1-2kb)                        2.37%
>   Promoter (2-3kb)                        1.89%
>   5' UTR                                  6.48%
>   3' UTR                                  3.62%
>   1st Exon                                3.13%
>   Other Exon                              4.65%
>   1st Intron                             10.56%
>   Other Intron                           18.53%
>   Downstream (<=300)                      0.14%
>   Distal Intergenic                      19.51%
> ```

**注释结果的关键解读：**

- **K562 的 Promoter 占比更高（29.1% vs 23.4%）**：K562 是癌细胞系，具有更多启动子近端的开放区域
- **GM12878 的 Intron 占比更高（36.6% vs 29.1%）**（13.1% + 23.5% vs 10.6% + 18.5%）：GM12878 有更多内含子增强子
- **Distal intergenic 占比相近（~19.5-19.8%）**：两种细胞都有大量远端增强子
- **Exon（外显子）占比较低（~7%）**：外显子区域通常被核小体覆盖，Tn5 切割较少

### 可视化：注释饼图和距离分布

```r
# ============================================================
# ChIPseeker 可视化
# ============================================================

# 1. 注释饼图（GM12878）
pdf("annotation/GM12878_peak_annotation.png", width = 8, height = 8)
plotAnnoPie(peakAnno_GM,
            col = c("#E41A1C","#377EB8","#4DAF4A","#984EA3",
                    "#FF7F00","#FFFF33","#A65628","#F781BF",
                    "#999999","#66C2A5","#FC8D62"))
dev.off()

# K562 注释饼图
pdf("annotation/K562_peak_annotation.png", width = 8, height = 8)
plotAnnoPie(peakAnno_K562,
            col = c("#E41A1C","#377EB8","#4DAF4A","#984EA3",
                    "#FF7F00","#FFFF33","#A65628","#F781BF",
                    "#999999","#66C2A5","#FC8D62"))
dev.off()

# 2. 距离 TSS 分布
pdf("annotation/distance_to_tss.png", width = 10, height = 6)
plotDistToTSS(peakAnno_GM,
              title = "Distribution of Peaks Relative to TSS (GM12878)",
              ylab = "Peaks (%)")
dev.off()

# 3. 按染色体的 peak 覆盖情况
pdf("peaks/consensus/peak_coverage_chr.pdf", width = 12, height = 6)
peaks_gr <- readPeakFile("peaks/consensus/consensus_peaks.bed")
covplot(peaks_gr, weightCol = "V5")
dev.off()
```

⚠️ **IDR 输出格式处理**：IDR 输出文件包含额外的列（超过 narrowPeak 的标准 10 列），直接传给 ChIPseeker 会报错。需要先截取前 10 列转换为标准 narrowPeak 格式：

```bash
# 将 IDR 输出转换为标准 narrowPeak 格式（取前 10 列）
for group in GM12878 K562; do
    cut -f1-10 idr/${group}_idr.txt > idr/${group}_idr.narrowPeak
done
```

```r
# 4. 比较两个条件的注释分布（使用上面已完成的注释结果）
peakAnnoList <- list(GM12878 = peakAnno_GM, K562 = peakAnno_K562)

pdf("annotation/feature_distribution.png", width = 10, height = 6)
plotAnnoBar(peakAnnoList)
dev.off()
```

<!-- 图 3 位置：Consensus peaks 注释饼图，各功能区域以不同颜色显示比例 -->

![](figures/GM12878_peak_annotation.png)

![](figures/K562_peak_annotation.png)


**图 3：GM12878 和 K562 的 peak 基因组注释。** K562 的启动子区占比更高（29.1% vs 23.4%），反映了癌细胞系更多启动子近端的开放区域。GM12878 有更多内含子 peaks（36.6% vs 29.1%），两种细胞的远端基因间区占比相近（~19.5-19.8%）。生成的注释图包括 GM12878_peak_annotation.png、K562_peak_annotation.png 和 feature_distribution.png。

<!-- 图 4 位置：Peaks 到 TSS 距离分布图，x 轴为距 TSS 距离，y 轴为 peak 百分比 -->

![](figures/distance_to_tss.png)


**图 4：Peaks 与 TSS 的距离分布。** 在 TSS 附近（±1 kb）有一个显著的富集峰，反映了启动子区域的高度开放性。随着距离 TSS 增大，peak 密度逐渐降低，但在 >100 kb 的远端仍有相当数量的 peaks，这些是远端增强子的信号。

---

## 第四部分：HOMER 注释（替代方案）

如果你更习惯命令行工具，HOMER 的 `annotatePeaks.pl` 是一个很好的替代选择。

```bash
# ============================================================
# HOMER Peak 注释（替代方案）
# ============================================================

# 需要先安装 HOMER 并配置 hg38 基因组
# perl /path/to/configureHomer.pl -install hg38

annotatePeaks.pl \
    idr/GM12878_idr.narrowPeak \
    hg38 \
    -annStats annotation/homer_annStats.txt \
    > annotation/homer_annotated.txt

# 提取注释统计
echo "=== HOMER 注释统计 ==="
head -30 annotation/homer_annStats.txt
```

> 📊 **输出：**
> ```
> === HOMER 注释统计 ===
> Annotation	Number	%
> Promoter-TSS	12767	28.0%
> 5' UTR	2599	5.7%
> Exon	3192	7.0%
> Intron	15959	35.0%
> TTS	684	1.5%
> Intergenic	10397	22.8%
> ```

**HOMER 和 ChIPseeker 的注释结果基本一致**，差异主要来自 promoter 区域的定义方式和优先级规则的不同。选择哪个工具主要看你的习惯和后续分析流程。

---

## 第五部分：GO/KEGG 富集分析

Peak 注释让我们知道了"在哪里"，但更重要的问题是"与什么生物学功能相关？" 通过对 peak 关联基因进行功能富集分析，可以把表观遗传学信号与生物学通路联系起来。

```r
# ============================================================
# GO/KEGG 富集分析（clusterProfiler）
# ============================================================

library(clusterProfiler)
library(org.Hs.eg.db)
library(enrichplot)

# 从 ChIPseeker 注释结果中提取关联基因
genes <- as.data.frame(peakAnno_GM)$geneId
genes <- unique(genes[!is.na(genes)])

cat("Peak 关联基因数量:", length(genes), "\n")

# GO Biological Process 富集
ego <- enrichGO(
    gene     = genes,
    OrgDb    = org.Hs.eg.db,
    keyType  = "ENTREZID",
    ont      = "BP",
    pAdjustMethod = "BH",
    pvalueCutoff  = 0.01,
    qvalueCutoff  = 0.05,
    readable = TRUE
)

# KEGG pathway 富集
ekegg <- enrichKEGG(
    gene     = genes,
    organism = "hsa",
    pvalueCutoff = 0.05
)

# 可视化
pdf("peaks/consensus/GO_dotplot.pdf", width = 10, height = 8)
dotplot(ego, showCategory = 20, title = "GO Biological Process Enrichment")
dev.off()

pdf("peaks/consensus/GO_cnetplot.pdf", width = 12, height = 10)
cnetplot(ego, showCategory = 5, cex_label_category = 0.8)
dev.off()

pdf("peaks/consensus/KEGG_dotplot.pdf", width = 10, height = 8)
dotplot(ekegg, showCategory = 20, title = "KEGG Pathway Enrichment")
dev.off()

# 分别对 GM12878 和 K562 特有 peak 关联基因做富集
# （这部分在第 9 篇差异分析后会更有意义）
cat("Top 10 GO terms:\n")
head(ego@result[, c("Description", "pvalue", "Count")], 10)
```

> 📊 **输出：**
> ```
> Peak 关联基因数量: 18453
>
> Top 10 GO terms:
>                          Description       pvalue Count
> 1     regulation of transcription...   2.3e-45   3856
> 2  regulation of RNA metabolic...      4.7e-38   3542
> 3      regulation of gene expression   8.1e-35   4012
> 4          cell-cell signaling        1.2e-28   1876
> 5        immune system process         3.5e-26   2345
> 6    regulation of cell communication  5.8e-24   3123
> 7   regulation of signal transduction  7.2e-22   2876
> 8        hemopoietic or lymphoid...    1.4e-20   856
> 9     leukocyte activation            2.8e-19   987
> 10     regulation of cell death        4.1e-18   1654
> ```

<!-- 图 5 位置：GO 富集分析 dotplot，展示 Top 20 GO BP 通路，点大小代表基因数，颜色代表 p 值 -->

![](figures/go_enrichment_gm12878_peaks.png)

**图 5：GO Biological Process 富集分析。** 转录调控、免疫系统过程和造血/淋巴发育相关通路显著富集。这与我们使用的细胞系完全吻合——GM12878（B 淋巴细胞系）和 K562（慢性髓系白血病细胞系）都是造血系统来源，其开放染色质区域自然富集在免疫和造血相关的调控元件上。

---

## 实用提示

### 1. TSS Region 的定义

`tssRegion = c(-3000, 3000)` 定义了 promoter 的范围。你可以根据需要调整：

| 定义 | 范围 | promoter 占比 |
|------|------|:------------:|
| 窄 | ±1 kb | ~22% |
| 标准 | ±3 kb | ~31% |
| 宽 | ±5 kb | ~37% |

**建议使用 ±3 kb**，这是社区最常用的标准。

### 2. 注释优先级

当一个 peak 同时落在多个功能区域时（比如一个基因的内含子同时也是另一个基因的启动子），ChIPseeker 会按照以下优先级分配：

**Promoter > 5'UTR > 3'UTR > Exon > Intron > Downstream > Intergenic**

### 3. IDR 阈值选择

| 阈值 | 通过 peaks (GM12878) | 适用场景 |
|:----:|:---------:|---------| 
| 0.01 | ~30,000 | 严格筛选，适合 motif 分析 |
| 0.05 | ~35,430 | 标准阈值（ENCODE 推荐） |
| 0.10 | ~40,000 | 宽松筛选，适合全面探索 |

---

## 本篇小结

| 步骤 | 关键点 |
|------|--------|
| IDR 分析 | 定量评估重复一致性，ENCODE 推荐阈值 0.05 |
| IDR 结果 | GM12878 35,430/45,623 (77.7%), K562 24,860/37,794 (65.8%) 通过 |
| Overlap 分析 | 重复间重叠率高，说明重复性良好 |
| ChIPseeker 注释 | GM12878: 27.2% promoter, 36.6% intron, 19.8% intergenic; K562: 33.4% promoter, 29.1% intron, 19.5% intergenic |
| HOMER 注释 | 结果一致，纯命令行替代方案 |
| GO/KEGG 富集 | 造血、免疫和转录调控通路富集 |

---

## 下一篇预告

我们已经知道了 peaks 在哪里、注释到了什么功能区域。接下来最激动人心的问题来了：**GM12878 和 K562 之间，到底哪些染色质区域的开放性发生了显著变化？** 第 9 篇将用 DiffBind 和 DESeq2 进行差异可及性分析——真正找到淋巴系和髓系/红系之间的表观遗传差异。

下篇见。

> 📌 **本篇代码和示例数据已上传至** [petemeng/ATAC-seq-Tutorial](https://github.com/petemeng/ATAC-seq-Tutorial)，欢迎 Star ⭐ 和提 Issue。

---

## 本系列导航

| 篇目 | 主题 | 状态 |
|:----:|------|:----:|
| 第 1 篇 | 染色质可及性 | ✅ 已发布 |
| 第 2 篇 | 环境搭建 | ✅ 已发布 |
| 第 3 篇 | 质控 | ✅ 已发布 |
| 第 4 篇 | 比对 | ✅ 已发布 |
| 第 5 篇 | 过滤 | ✅ 已发布 |
| 第 6 篇 | QC 指标 | ✅ 已发布 |
| 第 7 篇 | Peak Calling | ✅ 已发布 |
| 第 8 篇 | IDR 与注释 | **📍 本篇** |
| 第 9 篇 | 差异可及性 | 🔜 下一篇 |
| 第 10 篇 | Motif | 即将发布 |
| 第 11 篇 | Footprinting | 即将发布 |
| 第 12 篇 | chromVAR | 即将发布 |
| 第 13 篇 | 核小体 | 即将发布 |
| 第 14 篇 | 整合可视化 | 即将发布 |
